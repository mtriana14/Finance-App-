<?xml version="1.0" encoding="utf-8"?>
<!--
  On Android 12 and above, android:allowBackup="false" stops cloud backup but
  does NOT stop device-to-device transfer. Both channels are closed here.

  The database holds names, phone numbers and outstanding debts for people who
  are not users of this app and never agreed to anything. None of that should
  leave the phone by a route the merchant did not choose. When Phase 2 adds a
  server and SQLCipher, this stays as it is — an encrypted backup is a decision
  to be made deliberately, not inherited from a platform default.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
